The hero line "this line streamed in just for you — 20 Aug 2026, 05:30:33 UTC"
The first it streams in, it takes a while. We don't have a loading/suspended state for it, do we?

---

yes

---

back to the copy of caching & revalidation:
I still feel like it doesn't speak to my experience as a developer. The concept of caching and revalidation is not easy to digest for beginners, yes. But personally I have never had the "argument" on my mind that I revalidated some data and yet it remained stale. From USING the site, that never happened to me because I never even realized it's more than one event. In the observability at Vercel I did ask myself why some requests show the cache miss, why some are marked as stale. 

I'd like to nail the demo. It has to speak to something developers actually experience. And the argument, as the copy says, is not something any dev experienced except if they accidentally built an impure function or mutated data during a GET request. Otherwise, it's looks and feels like one event.

---

Full P3.

However, the title "It’s either cached or it isn’t" and the start of P1 "So did we" does not add up. No belief was expressed explicitly enough to continue with "So did we".

---

is the bake and actions.ts up to date with our demo?

---

yes

---

the comment "The bake is a random id" should read "The bake here is a random color value"

---

<system_instruction>
The user has attached these files. Read them before proceeding.
- .context/attachments/MQb3O0/Screenshot 2026-08-20 at 08.13.04.png (96.5 KB)
</system_instruction>



.context/attachments/MQb3O0/Screenshot 2026-08-20 at 08.13.04.png 

This is a screenshot of the logf of the GET request I just made to the preview deployment after the rebake. Even in the observability there's no STALE marker. Reconcile this with our demo and copy. You can log into Vercel yourself and read logs, observe what happens on the preview deployment. I'd think that STALE is a state that only shows for revalidateTag, not for updateTag.

---

Think about how best to integrate that into the demo, the badges for revalidated and the tag. Also, the copy has to change here, too: the hash above was rendered before your expiry landed. The hash is now a hex color. Maybe just say color?

---

Go

---

Commit the changes.

There's a gap in our UI I think: After clicking Expire the entry, the second button becomes clickable immediately. Should we close it or keep it intentionally since it skips the private render?

---

commit and push

---

In these sentences "the fetch ran no cache API — the page rendered again, and this time the cache kept it. Expiring an entry and refilling it are two different events. You just watched both." remove the last one. It reads like AI generated prose.

---

commit the change. before you push, fix the issue the build just experienced:
web:typecheck: app/[locale]/(playground)/shell/rebake-copy.test.ts(30,7): error TS2322: Type '{ expiredAt: string; hasCompletedCycle: false; mode: "machinery"; phase: "expired"; }' is not assignable to type 'RebakeState'.
web:typecheck:   Property 'idAtExpiry' is missing in type '{ expiredAt: string; hasCompletedCycle: false; mode: "machinery"; phase: "expired"; }' but required in type '{ expiredAt: string; hasCompletedCycle: boolean; idAtExpiry: string; mode: "machinery"; phase: "expired"; }'.
web:typecheck: error: script "typecheck" exited with code 1
web#typecheck:  ERROR  command (/vercel/path0/apps/web) /bun1/bun run typecheck exited (1)

---

Let's apply our design changes to the other demos.

---

I have now set the checkpoints repo public, the address is https://github.com/hasToggle/hasToggle.dev-checkpoints
Adjust our landing page copy to link to it.

---

In the FAQ, "The repo is on GitHub" should link to the repo. Not the whole text, maybe "The" can be text and the remaining bits is the link. And we should add a GitHub icon to our social links and link the main repo.

---

Let's revisit the asterisk "You’ve opened a “live demo” that turned out to be an animated GIF. So have we. The demos below answer back, and the code that made them is one click down." 

Does it fit our voice? It doesn't speak to me.

---

No, it's too much prose for what's essentially a tooltip.

---

I think we should make the Contents navigation sticky after scrolling past it, so the user can navigate between demos more easily.

---

yes

---

On the desktop, it's not centered and looks off.

---

the entire nav bar is not centered, I mean. It sits left-bound, which aligns with the hero, but it should align with the content instead. And the content is centered.

---

The "contents" piece is itself not navigation link. I'd prefer the first item to be the link. "Contents" should drift leftward, off-center with the first nav link centered.

---

Is "The Shell" still accurate in describing the second demo?

---

No, "bake" is not terminology recognized by developers. We should be precise.

---

Apply some Brain Greene to the other demos; the second one already has some of it. Come back with a before and after, so the suggested changes are clear to me.

---

Demo 1:
"Only one of them knows what version of Node it is running on."
Ye, the browser does not run any Node at all. That's confusing to talk about versions of Node then.

Demo 3:
"Nothing waits for everything." Confusing wording.

---

commit and push

---

"Tell me when seats open" should set the focus to the input field. 
Plus, once it's been pressed it longer navigates to the digest. It should.

---

commit and push