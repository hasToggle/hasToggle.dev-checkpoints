<system_instruction>
You are working inside Conductor, a Mac app that lets the user run many coding agents in parallel.
Your work should take place in the /Users/eric/conductor/workspaces/spinor/tacoma directory (unless otherwise directed), which has been set up for you to work in.
Each workspace has a .context directory (gitignored) where you can save files to collaborate with other agents.
The target branch for this workspace is origin/main. Use this for actions like diffing (`git diff origin/main...`) or creating PRs (`gh pr create --base main`).

Do not rename the current branch unless the user explicitly tells you to do so.

By default, the user will only see the last message that you send before stopping. Include all essential information in the last message. The intermediate messages will be collapsed and accessible by the user but not displayed by default.

If the user asks you to work on several unrelated tasks, you can suggest they start new workspaces.
Sometimes the user might send you a message they meant to send in a different workspace or a different chat. If something doesn't make sense in the context of your work, just ask. 
If the user asks for help with Conductor, you can ask them to go to "Help -> Send Feedback" to get in touch with our team.
</system_instruction>

this repo is a fork of hasToggle where I have created this PR:

https://github.com/hasToggle/hasToggle.dev/pull/292

read it, understand it, then apply it in this repo instead. I will close the PR 292 in hasToggle once you have moved/reapplied all the code. I want hasToggle to become a Next.js & Vercel playground. The company brain product should be its own repo. I called the new product spinor.

---

<system_instruction>
The user has attached these files. Read them before proceeding.
- .context/attachments/oAyl8c/PR instructions.md (2.0 KB)
</system_instruction>



Create a PR .context/attachments/oAyl8c/PR instructions.md

---

wh could spinor as a name be appropriate to the idea of building a company brain? I'm kind of fascinated by the two rotations needed to start from the origin detail.

---

I don't have Renovate on that spinor repo anymore. Help me self-host a Renovate app and add to this repo (the Renovate app on hasToggle is managed by the Renovate Cloud Runner).

---

Remove .github/workflows/claude-code-review.yml 
I don't need PR reviews; I only review locally.

what's more, I couldn't run the button you provided, so I'm adding the Renovate App myself. But I can't find some of the permissions, like  statuses: "write",
          vulnerability_alerts: "read"

visit https://github.com/organizations/hasToggle/settings/apps/new yourself and inspect the permissions available to confirm your default_permissions.