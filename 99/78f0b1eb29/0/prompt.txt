earlier we tried preventing preview deployments by vercel when entire.io pushes meta data to the entire/checkpoints/v1 branch and we added a conditional in an existing no-ci script. however, that script never seems to take effect. see the screenshot and identify the individual elements from the vercel deployments ui. get familiar with the changes we tried and find another way to prevent these preview deployments.